#include<iostream>
using namespace std;
int main(){
   string str="Hello World !!";
   char ch;
   int len=str.length();
   for(int i=0; i< len/2; i++){
    ch=str[i];
    str[i]=str[len-i-1];
    str[len-i-1]=ch;
   }
   cout<<str<<endl;

}
